# Tests

Add lightweight checks here to validate:
- schema integrity of `data/processed/years_baseball.csv`
- expected column names for modeling features
- basic sanity checks (no duplicate game IDs, valid date ranges)

Use `testthat` for unit tests and simple R scripts for data validation as the project grows.
